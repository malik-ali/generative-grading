{
  "exp_name": "pyramid_convnet_200",
  "agent": "PyramidConvNet",

  "cuda": true,
  "gpu_device": 8,
  "seed": 1337,

  "model_type": "conv",
  "knowledge_states": false,

  "data_loader_workers": 4,
  "batch_size": 64,  

  "save_model": true,
  "display_progress": true,
  "validations_per_epoch": 0,
  "early_stopping_patience": 3,

  "learning_rate": 1e-5, 
  "weight_decay": 1e-7,
  "num_epochs": 100,

  "num_train": 200,
  "grammar_data": false,

  "encoder_kwargs": {
    "dropout": 0,
    "input_size": 64
  }
}
